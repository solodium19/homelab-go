controller:
 metrics:
  port: 10254
  enabled: true
  serviceMonitor:
   enabled: true
   namespace: monitoring
   scrapeInterval: 15s
   additionalLabels:
    release: kube-prometheus-stack
    app.kubernetes.io/instance: kube-prometheus-stack
